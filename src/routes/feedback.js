const router=require('express').Router();const {z}=require('zod');
const feedback=z.object({bookingId:z.string().optional(),guideId:z.string().optional(),rating:z.number().int().min(1).max(5),comment:z.string().max(1000).optional(),crowdLevel:z.number().int().min(1).max(5).optional(),spotId:z.string().optional()});
router.post('/',async(req,res,next)=>{try{const data=feedback.parse(req.body),prisma=req.app.get('prisma');const item=await prisma.feedback.create({data:{userId:req.user.sub,bookingId:data.bookingId,guideId:data.guideId,rating:data.rating,comment:data.comment,crowdLevel:data.crowdLevel}});if(data.spotId&&data.crowdLevel)await prisma.crowdReport.create({data:{spotId:data.spotId,level:data.crowdLevel}});if(data.guideId){const avg=await prisma.feedback.aggregate({where:{guideId:data.guideId},_avg:{rating:true}});await prisma.guideProfile.update({where:{id:data.guideId},data:{rating:avg._avg.rating||0}})}res.status(201).json(item)}catch(e){next(e)}});
module.exports=router;
